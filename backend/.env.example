DATABASE_URL=sqlite:///./smartcity.db
# Generate your own: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=changeme-generate-your-own-secret-key
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440
# Optional: https://huggingface.co/settings/tokens - only needed for the hosted
# severity-classification call in app/services/ai_service.py. Leave blank to
# fall back to "Medium" severity (the local AI models in ai_module/ don't need it).
HF_TOKEN=
